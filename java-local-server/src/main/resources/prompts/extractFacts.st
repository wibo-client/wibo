请基于参考信息里的内容，提取有助于回答 用户问题 的关键事实。

参考信息：
{references}

要求：
1. 回答中尽可能使用原文内容，包含详细的事实、数据、URL地址和观点判断等有效信息，不要漏掉事实，数据和连接。
2. 额外给出依据的id（即参考信息中json 中的 id 字段中的值），给出id字段必须是一个数值
3. 要尽可能保留全部有关的详细内容，不要丢失任何有关系的细节，包括详细的代码，数据，事实等。
4. 用 JSON 格式返回答案，格式如下：
\{
  "answer": [
    \{
      "fact": "提取的关键事实1，各类其他相关有效信息等",
      "id": "对应的id",
    \},
    \{
      "fact": "提取的关键事实2，各类其他相关有效信息", 
      "id": "对应的id2"
    \}
  ]
\}
4. 如参考信息不足以回答问题返回空的Answer json对象，格式如下：
\{
  "answer": []
\}

5. 严格按照用户要求处理请求，不需要有其他任何解释说明性反馈。

用户要求：
{question}