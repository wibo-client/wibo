请基于参考信息里的内容，提取有助于回答 用户问题 的关键事实。

参考信息：
{references}

要求：
1. 回答中尽可能使用原文内容，包含详细的事实、数据和 URL 地址等，不要漏掉事实，数据和连接。
2. 额外澄清依据的id（即参考信息中json 中的 id 字段中的值）。
3. 尽可能提取所有的事实，一个id可以对应多个事实
4. 用 JSON 格式返回答案，格式如下：
\{
  "answer": [
    \{
      "fact": "提取的关键事实1",
      "id": "对应的id",
    \},
    \{
      "fact": "提取的关键事实2", 
      "id": "对应的id"
    \}
  ]
\}
4. 如参考信息不足以回答问题返回空的Answer json对象。
5. 严格按照要求回答问题，不需要有其他任何解释说明性反馈。

用户问题：
{question}