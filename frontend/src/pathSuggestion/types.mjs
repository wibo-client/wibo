/**
 * @typedef {Object} PathConfig
 * @property {string} fullPath - 完整路径
 * @property {string[]} [keywords] - 关联的关键词
 * @property {string} [description] - 路径描述
 */

export const PathConfig = {};  // 仅作为类型命名空间使用
